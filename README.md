# lampa-ts

Lampa plugin: fixes torrent size (TorrServer `GCiB` units) and average bitrate display.

Install in Lampa: Settings -> Extensions -> Add plugin -> `https://evgeniipsac-source.github.io/lampa-ts/ts.js`
